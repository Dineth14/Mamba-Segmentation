# Segmentation

This directory is part of the model submodule tree used in this repository.

## Purpose
- provides task-specific code for segmentation workflows
- supports reproducible experiments in the corresponding parent model

## Usage
Run commands from the parent project root so configs and imports resolve correctly.

## Notes
This folder is maintained as part of a larger model codebase. Prefer modifying parent-level configs and scripts unless you need low-level changes here.
